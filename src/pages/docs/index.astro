---
import DocsLayout from '../../layouts/DocsLayout.astro';
import { getDocNav, getDocPage } from '../../lib/docs';

const nav = getDocNav();
const doc = await getDocPage('README');
---

<DocsLayout doc={doc} nav={nav} />
